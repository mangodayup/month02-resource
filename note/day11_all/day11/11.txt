前情回顾

1. 网络编程

   网络的功能 ： 分享和传输数据
   OSI七层模型： 网络通信的标准化流程模型
   TCP/IP模型： 在使用工作中使用 （四层）
   网络通信协议 ： 通信中各方遵守的通信规定
   网络地址 ： IP地址  端口号
   C/S结构 ： 客户端  服务端

cookie：
  这是什么
  具体描述
  特点说明 （优点，缺点）
  话题引申
  实际应用 （我用它做了什么）

2. socket 套接字编程

   udp套接字
   服务端
   socket()->bind()->recvfrom/sendto->close()

   客户端
   socket()->sendto/recvfrom->close()

3. tcp传输服务特点

   * 面向连接
   * 可靠的数据传输
   * 三次握手   四次挥手
   * 比udp麻烦，效率比udp低

练习01：
在源程序基础上修改，
让客户端可以循环的输入发送内容给服务端
收到服务端回复内容，直到发送 ## 两边都退出

练习02：
从客户端，将一个图片发送到服务端存储
图片自选，服务端将图片存储为 "20210219.jpg"

思路提示： 客户端  读取 --》 发送
          服务端  接收 --》 写入

练习03：
在示例代码基础上修改，
客户端退出后，服务端不退出，可以继续处理
下一个客户端连接访问

练习04：
有一个列表，列表中有一些名字，将这些名字
从客户端发送给服务端，如果名字里有 “张三”
则服务端返回一个 "存在VIP" 如果没有则
返回 "不存在VIP"


作业：
1. tcp重点代码 能够独立完成

2. 使用tcp完成一个会话小程序

问答小字典
{"key":"value",........}
key: 几岁
value : 我两岁了

从客户端 发送个问题给服务端，服务端根据
字典比对关键词回复问题，客户端收到回复
打印。
如果问题无法回复，统一回答 "人家还小不知道啦"




